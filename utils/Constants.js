// =====================================================
// Constants.js
// Constantes globales utilizadas en toda la simulación.
// =====================================================

// Ancho del canvas.
export const WIDTH = 660;

// Alto del canvas.
export const HEIGHT = 420;

// Número inicial de partículas.
export const DEFAULT_PARTICLES = 500;

// Velocidad inicial de las partículas.
export const DEFAULT_SPEED = 1.5;

// Radio inicial de cada partícula.
export const DEFAULT_RADIUS = 4;

// Capacidad máxima de partículas por nodo antes de subdividir.
export const DEFAULT_CAPACITY = 4;

// Radio utilizado para consultar vecinos.
export const DEFAULT_NEIGHBOR_RADIUS = 40;

// Cantidad máxima permitida de partículas.
export const MAX_PARTICLES = 10000;
